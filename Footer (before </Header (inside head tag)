<style>
/* ============================================================
   MOTORO AUTOMOTIVE GROUP — THEME SYSTEM
   Default: DARK | Toggle: data-theme="light" on <html>
   ============================================================ */

/* ---------- CSS VARIABLES ---------- */
:root,
html[data-theme="dark"] {
  --bg:          #0a0a0a;
  --bg-card:     #111111;
  --bg-card-2:   #1a1a1a;
  --border:      rgba(255,255,255,0.08);
  --text:        #e8e8e8;
  --text-muted:  #888888;
  --accent:      #c8102e;
  --accent-dark: #a00d24;
  --nav-bg:      #0a0a0a;
  --nav-text:    #e0e0e0;
  --btn-text:    #ffffff;
}

html[data-theme="light"] {
  --bg:          #f5f5f5;
  --bg-card:     #ffffff;
  --bg-card-2:   #f0f0f0;
  --border:      rgba(0,0,0,0.10);
  --text:        #1a1a1a;
  --text-muted:  #666666;
  --accent:      #c8102e;
  --accent-dark: #a00d24;
  --nav-bg:      #ffffff;
  --nav-text:    #1a1a1a;
  --btn-text:    #ffffff;
}

/* ---------- GLOBAL RESET ---------- */
*, *::before, *::after { box-sizing: border-box; }

html {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
  background-color: var(--bg);
  color: var(--text);
  scroll-behavior: smooth;
}

body {
  background-color: var(--bg) !important;
  color: var(--text) !important;
  margin: 0;
  padding: 0;
}

/* ---------- NAVBAR ---------- */
header, nav, .navbar, .site-header, #masthead,
.dws-header, .dws-topbar, .top-bar, .dws-top-bar {
  background-color: var(--nav-bg) !important;
  border-bottom: 1px solid var(--border) !important;
}

.navbar .nav-link, .navbar a, .nav-menu a, .dws-nav-menu a {
  color: var(--nav-text) !important;
  transition: color 0.2s ease;
}

.navbar .nav-link:hover, .navbar a:hover,
.nav-menu a:hover, .dws-nav-menu a:hover,
.navbar .nav-link.active, .current-menu-item > a {
  color: var(--accent) !important;
}

@media (min-width: 992px) {
  .navbar-collapse, .dws-nav-collapse {
    display: flex !important;
    visibility: visible !important;
    height: auto !important;
    overflow: visible !important;
  }
  .navbar-toggler, .menu-toggle, .dws-menu-toggle { display: none !important; }
  .navbar-nav, .dws-nav-menu { display: flex !important; flex-direction: row !important; align-items: center !important; }
  .navbar-nav > li > a { padding: 8px 14px !important; font-size: 14px !important; white-space: nowrap !important; }
}

/* ---------- TYPOGRAPHY ---------- */
h1,h2,h3,h4,h5,h6 { color: var(--text) !important; }
p, span, li, td, th { color: var(--text) !important; }
a { color: var(--text) !important; }
a:hover { color: var(--accent) !important; }
.text-muted, .text-secondary { color: var(--text-muted) !important; }

/* ---------- CARDS ---------- */
.card, .dws-card, .inv-card, .vehicle-card {
  background-color: var(--bg-card) !important;
  border-color: var(--border) !important;
  color: var(--text) !important;
}

.card-title, .dws-vehicle-title { color: var(--text) !important; font-weight: 700 !important; }
.card-title:hover, .dws-vehicle-title:hover { color: var(--accent) !important; }

.dws-vehicle-price, .vehicle-price, .inv-price, .sale-price, .internet-price {
  color: var(--accent) !important;
  font-weight: 700 !important;
}

/* ---------- BUTTONS ---------- */
.btn-primary, .dws-btn-primary, .btn-danger, .wp-block-button__link,
input[type="submit"], button[type="submit"] {
  background-color: var(--accent) !important;
  border-color: var(--accent) !important;
  color: var(--btn-text) !important;
}

.btn-primary:hover, input[type="submit"]:hover, button[type="submit"]:hover {
  background-color: var(--accent-dark) !important;
  border-color: var(--accent-dark) !important;
}

/* ---------- FORMS ---------- */
input, select, textarea, .form-control, .form-select {
  background-color: var(--bg-card-2) !important;
  color: var(--text) !important;
  border-color: var(--border) !important;
}
input:focus, textarea:focus, select:focus {
  border-color: var(--accent) !important;
  box-shadow: 0 0 0 2px rgba(200,16,46,0.2) !important;
  outline: none !important;
}

/* ---------- PAGINATION ---------- */
.pagination .page-link { color: var(--text) !important; background: var(--bg-card) !important; border-color: var(--border) !important; }
.pagination .active .page-link { background: var(--accent) !important; border-color: var(--accent) !important; color: #fff !important; }

/* ---------- BADGES ---------- */
.badge { background-color: var(--accent) !important; color: #fff !important; }

/* ---------- THEME TOGGLE BUTTON ---------- */
#motoro-theme-toggle {
  position: fixed;
  top: 14px;
  right: 16px;
  z-index: 99999;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: none;
  cursor: pointer;
  background: var(--bg-card);
  color: var(--text);
  box-shadow: 0 2px 12px rgba(0,0,0,0.35);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  transition: background 0.25s, transform 0.2s, box-shadow 0.25s;
  -webkit-tap-highlight-color: transparent;
}
#motoro-theme-toggle:hover {
  transform: scale(1.1);
  box-shadow: 0 4px 20px rgba(200,16,46,0.4);
}

/* ---------- HIDE DC DEFAULT FOOTER ---------- */
.dws-footer, .dws-footer-wrapper, .dws-footer-container,
#dws-footer, .footer-wrapper, #footer-wrapper,
.site-footer, #site-footer {
  display: none !important;
}

footer {
  display: none !important;
}

#motoro-footer-custom {
  display: block !important;
  visibility: visible !important;
}
</style>
